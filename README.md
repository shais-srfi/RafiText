# RafiText

A small text editor project based off the Kilo tutorial, implemented in C++ on Linux/WSL.

## Requirements (WSL / Linux)

- Ubuntu (WSL2) or Linux
- g++
- git
- cmake (optional)
- ninja (optional)

## Build
```bash
cmake -S . -B build -G Ninja
cmake --build build
```